"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mockSessionFromFunction = void 0;
const neo4j = require('neo4j-driver');
const mockDatabaseInfo = {
    URI: 'neo4j+s://84751e18.databases.neo4j.io',
    USER: 'faker',
    PASSWORD: 'thisIsAfakeDatabase',
};
function mockSessionFromFunction(sessionRunMock) {
    const driver = neo4j.driver(mockDatabaseInfo.URI, neo4j.auth.basic(mockDatabaseInfo.USER, mockDatabaseInfo.PASSWORD));
    const fakeSession = driver.session();
    fakeSession.run = sessionRunMock;
    return fakeSession;
}
exports.mockSessionFromFunction = mockSessionFromFunction;
module.exports = mockSessionFromFunction;
